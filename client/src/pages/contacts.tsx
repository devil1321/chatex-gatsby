import React from 'react'
import { GlobalComponents } from '../components/global'

const Contacts = () => {
  return (
    <GlobalComponents.Layout title='Contacts' className='contacts'>
      <div>contacts</div>
    </GlobalComponents.Layout>
  )
}

export default Contacts
